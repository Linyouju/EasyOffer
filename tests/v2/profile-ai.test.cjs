const assert=require('node:assert/strict');global.ProfileCore=require('../../integrations/openjobtracker/v2/profile-core');const A=require('../../integrations/openjobtracker/v2/profile-ai');const p=ProfileCore.empty();ProfileCore.command(p,{operationId:'init',changes:[{recordId:'P1',addRecord:{type:'project'}},{recordId:'P1',fieldId:'name',newField:{key:'name',label:'项目名称',value:'预约研究'}},{recordId:'P1',fieldId:'desc',newField:{key:'description',label:'完整原始描述',value:'旧描述'}},{recordId:'P1',fieldId:'date',newField:{key:'start',label:'开始日期',value:'2025-08-26'}}]});let output;global.ModelGateway={invoke:async()=>output};
(async()=>{const text='预约研究的描述替换为：\n访谈预约用户。\n完成原型验证。';output={changes:[{recordId:'P1',fieldId:'desc',value:'访谈预约用户。\n完成原型验证。',evidence:'访谈预约用户。\n完成原型验证。'}]};const result=await A.propose(p,text,'edit',{});assert.equal(result.changes.length,1);ProfileCore.command(p,{operationId:'ai',changes:result.changes});assert.equal(p.records[0].fields.find(f=>f.id==='desc').value,'访谈预约用户。\n完成原型验证。');
output={changes:[{recordId:'P1',fieldId:'date',value:'2025-08',evidence:'2025年8月'}]};assert.equal((await A.propose(p,'2025年8月开始预约研究','resume',{})).changes.length,0,'resume month does not replace full date');
output={changes:[{recordId:'P1',fieldId:'desc',value:'效率提升50%',evidence:'完成原型'}]};await assert.rejects(A.propose(p,'完成原型','resume',{}),/原文依据/);
output={ambiguous:true,reason:'同名项目有两条'};assert((await A.propose(p,'修改预约研究','edit',{})).ambiguous);console.log('PASS AI Profile patch: original replacement, scoped edit, date precision, unsupported claim refusal, ambiguity');})().catch(e=>{console.error(e);process.exitCode=1});

assert.equal(A.supported('', '新增一个项目', '新增一个项目'),false,'empty model output cannot erase an unmentioned fact');
