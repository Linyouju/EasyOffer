const assert=require('node:assert/strict'),C=require('../../integrations/openjobtracker/v2/application-core');global.XLSX=require('../../vendor/sheetjs/xlsx.full.min');global.ApplicationCore=C;const E=require('../../integrations/openjobtracker/v2/excel-export');
const s=C.empty(),o={id:'a',site:'https://example.test',observedAt:1000,pageKind:'application',sourceApplicationId:'APP1',posting:{company:'示例企业',title:'交互设计',season:'2027'},status:{submitted:true,raw:'资料处理中',appliedAt:'2026-09-20'},evidence:[{regionId:'r',role:'application',text:'已投递 资料处理中 投递日期 2026-09-20'}]};const id=C.observe(s,o).result.applicationId;assert.equal(C.toView(s.applications[0]).status,'已投递');assert.equal(C.toView(s.applications[0]).appliedAt,'2026-09-20');const count=s.events.length;C.observe(s,{...o,id:'b',observedAt:2000});assert.equal(s.events.length,count);assert.equal(s.applications[0].lastStatusObservedAt,2000);C.observe(s,{...o,id:'b',observedAt:3000});assert.equal(s.applications[0].lastStatusObservedAt,3000);
const before=structuredClone(s.applications[0]);C.command(s,{operationId:'local',applicationId:id,baseRevision:before.revision,patch:{notes:'本地编辑'}});assert.equal(s.applications[0].lastStatusObservedAt,3000,'local edit is not website check');const view=C.toView(s.applications[0]),rows=XLSX.utils.sheet_to_json(E.workbook(s).Sheets['EasyOffer权威记录']);assert.equal(rows[0].applicationId,view.applicationId);assert.equal(rows[0]['投递日期'],view.appliedAt);assert.equal(rows[0]['备注'],'本地编辑');
const noDate=C.empty();C.observe(noDate,{...o,id:'no',status:{...o.status,appliedAt:'2026-09-21'}});assert.equal(C.toView(noDate.applications[0]).appliedAt,'','unsupported date not invented');console.log('PASS Application dates: unknown state keeps submitted, evidence date, repeat observations refresh time without event, local edits not website checks, exports agree');
